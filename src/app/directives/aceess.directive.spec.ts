import { AceessDirective } from './aceess.directive';

describe('AceessDirective', () => {
  it('should create an instance', () => {
    const directive = new AceessDirective();
    expect(directive).toBeTruthy();
  });
});
